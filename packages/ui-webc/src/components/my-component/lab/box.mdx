# Box Example

## Loops

{['a', 'b', 'c'].map(item => (
  <my-component first={item} last="!" />
))}

## Object props

<my-component
  first="Stencil"
  middle="'Don't call me a framework'"
  last="JS"
  test={{
    name: 'dummy'
  }}
/>
