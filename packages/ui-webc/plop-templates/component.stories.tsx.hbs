import { h } from "@stencil/core";
import type { Meta, StoryObj } from "@stencil/storybook-plugin";
import { {{pascalCase name}} } from "./{{name}}";

const meta: Meta<{{pascalCase name}}> = {
  title: "{{pascalCase name}}",
  component: {{pascalCase name}},
  parameters: {
    layout: "centered",
  },
  argTypes: {
    variant: {
      control: { type: "select" },
      options: ["default"],
    },
  },
  render: (props) => <{{name}} {...props}>Content</{{name}}>,
};

export default meta;

type Story = StoryObj<{{pascalCase name}}>;

export const Default: Story = {
  args: {
    variant: "default",
  },
  render: (props) => <{{name}} {...props}>Default example</{{name}}>,
};
