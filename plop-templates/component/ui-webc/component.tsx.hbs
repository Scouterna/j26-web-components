import { Component, h } from "@stencil/core";

@Component({
  tag: "scout-{{name}}",
  styleUrl: "{{name}}.css",
  shadow: {
    delegatesFocus: true,
  },
})
export class Scout{{pascalCase name}} {
  render() {
    return (
      <slot />
    );
  }
}
